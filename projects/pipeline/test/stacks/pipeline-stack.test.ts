import * as cdk from 'aws-cdk-lib';

describe('NetworkStack', () => {
  let app: cdk.App;
  // let stack: PipelineStack;
  // let template: Template;

  // beforeEach(() => {
  //   app = new cdk.App({
  //     outdir: 'cdk.out'
  //   });
  // });

  test('PipelineStack Created', () => {});
});
